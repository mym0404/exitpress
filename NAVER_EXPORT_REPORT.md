# Naver 내보내기와 블로그 이관 렌더링 문제

2026-09-13에 `mym0404` 내보내기 결과와 원본 Naver HTML, ps-blog의 실제 렌더링을 비교했어요. 이 문서는 발견한 문제, 원인, 수정 범위와 검증 결과를 기록해요.

## 조사 범위

- 내보내기 manifest에는 성공한 글 1,935개가 있고, 원본 폴더에는 Markdown 파일 1,934개가 있어요.
- ps-blog에는 1,830개를 이관했어요. 기존 글과 중복인 85개, 링크만 있는 17개, 빈 글 2개를 제외했어요. 중복 글 중 44개는 기존 ps-blog 글로 연결되는 카드이고, 그중 43개가 알고리즘 글이에요.
- 이관한 원본 카테고리 71개에서 각각 3개를 골랐어요. 글이 3개보다 적은 13개 카테고리는 전부 골라 총 191개를 검사했어요.
- 191개를 데스크톱 1,440 × 1,000과 모바일 390 × 844에서 열고 화면을 확인했어요. 총 382회 검사에서 25개 글의 문제 26건을 찾았어요. 한 글은 비트 OR와 이미지 문제가 겹쳐요.
- 이후 같은 유형을 이관한 1,830개 전체에서 조사했어요. 표본에서 보이지 않았던 수식 줄 누락과 영상 메타데이터 누락도 원본 HTML에서 확인했어요.

## 원인과 영향

| 문제 | 재현 글과 관찰한 결과 | 원인과 수정 위치 |
| --- | --- | --- |
| 설명용 각괄호 뒤의 코드·이미지 누락 | `221008605391`, `221006928501`, `220986701555`, `221360784265`, `221179506545`, `221231819416`. 이관 Markdown의 코드 79개 중 21개만 코드 블록으로 렌더링됐어요. | 일반 텍스트의 `<...>`가 HTML로 해석돼 이후 Markdown까지 먹었어요. 공통 HTML→Markdown 변환의 텍스트 경계에서 보호해요. 코드와 실제 HTML은 보존해요. |
| 제네릭 타입 누락 | `221427915590`의 `Range<String.Index>`에서 타입 인수가 사라졌어요. | 위와 같은 텍스트 이스케이프 누락이에요. |
| 코드 들여쓰기·빈 줄 손상 | HTML 변환 뒤 문단을 만드는 실제 경로에서 들여쓰기가 한 칸으로 줄고, 빈 줄·fence·구분선이 사라지는 것을 회귀 테스트로 재현했어요. | 이미 만들어진 Markdown 전체에 일반 문장용 공백 압축을 다시 적용했어요. 중복 압축을 제거하고 구조를 보존해요. |
| ColorScripter 코드 치환 충돌 | 코드의 `$&`, 코드 내부 백틱, 11개 이상 코드 블록의 placeholder를 각각 재현했어요. | 문자열 치환의 특수 토큰, 고정 길이 fence, 인덱스 접두사 충돌이 원인이에요. 함수 치환·충분히 긴 fence·전체 인덱스 치환으로 고쳐요. |
| 비트 OR가 표로 바뀜 | `221230310821`의 `return x \| mask`와 복합식이 두 열의 표가 됐어요. | 일반 텍스트의 pipe가 GFM 표 구문으로 해석됐어요. 텍스트 경계에서 보호하고 실제 표 셀은 이중 이스케이프하지 않아요. |
| 셸 명령을 수식으로 처리 | `221420560667`의 `$ git rm ... $ git add ...`가 수식으로 바뀌었어요. | 리터럴 달러가 Markdown 수식 구분자와 충돌했어요. GFM 출력은 리터럴을 이스케이프하고, Jekyll 이관본의 셸 프롬프트는 코드로 표시해요. |
| 링크 카드 구문 노출 | `221317601407`에서 대괄호와 링크 대상이 본문에 그대로 보였어요. | 링크 라벨 안의 빈 줄이 Markdown 링크를 끊었어요. 라벨의 줄을 한 줄로 연결해요. |
| 링크 프리뷰의 TeX 구분자 손실 | `222616745545`의 외부 글 요약에서 TeX 구분자가 사라졌어요. | 원본 카드 미리보기의 TeX·대괄호·달러가 Markdown 구문으로 재해석됐어요. 일반 텍스트를 이스케이프해 원문 문자를 보존해요. 원본 Naver 카드도 문자로 표시하므로 엔진에서 수식으로 자동 변환하지 않아요. |
| API 표에 wbr 태그 노출 | `221458178778`에서 `<wbr style="box-sizing: inherit;" />`가 글자로 보였어요. | 변환 후 문자열 전체를 이스케이프하면서 실제 HTML 표를 훼손했어요. 이스케이프를 원본 텍스트 노드에 한정해요. |
| SQL 코드·출력의 줄 합침 | `221392997287`의 ColorScripter 코드·출력 6개가 표로 바뀌고 열 정렬이 깨졌어요. | 1행 2셀의 코드와 `cs` 출처 링크 구조를 일반 표로 처리했어요. 이 구조를 먼저 코드로 추출해 줄·공백·pipe를 보존해요. 원본 1,935개에서 이 조건에 맞는 것은 해당 글의 6개뿐이에요. 일반·복합 표는 그대로 처리해요. |
| 여러 줄 수식 중 한 줄만 남음 | `222452260477`의 수식 컴포넌트에서 원본 20줄 중 한 줄만 남았어요. | FormulaBlock이 `.mq-selectable` 후보 중 가장 긴 하나만 골랐어요. 원본 순서대로 각 수식을 보존해요. |
| Naver 수식 명령 호환성 | `\combi`, `\overrightharpoonup`, `\nequiv`, `\lcup`, `\nin`, `\normal`, `grid`와 제어문자가 포함된 수식에서 변환·렌더링 문제가 있었어요. | 원본 기호를 대조해 표준 TeX로 바꾸고 제어문자를 제거해요. `grid`는 셀별 경계선을 보존한 배열로 바꾸고 끝의 이스케이프된 공백을 유지해요. |
| 영상이 ‘재생’ 텍스트로 빠짐 | `221312876679`, `221219476787`, `221355426801`에서 영상 정보가 사라졌어요. | 기존 SE2 파서는 iframe 중심 구조를 처리했지만 현재 `pzp-mobile-layout._naverVideo`와 `pzp-pc-layout._naverVideo`를 놓쳤어요. 해당 구조의 원본 링크·영상 ID·썸네일을 보존하고 만료 키는 출력하지 않아요. |
| 이미지가 다운로드·업로드에서 빠짐 | 본문 인라인 이미지와 영상 썸네일이 선택한 자산 처리 정책을 우회했어요. | SE2 문단·인용·제목과 표 내부 이미지, 영상 썸네일의 자산 등록 누락을 고쳤어요. 기존 다운로드·업로드·원본 URL 정책을 적용하며, 이미지 제외 시 주변 본문과 링크를 보존해요. |
| 외부 썸네일 차단 | 8개 글의 dthumb 이미지 9개가 실제 브라우저에서 로드되지 않았어요. TensorFlow 2개는 `no-referrer`로 재요청하면 표시됐어요. | 참조자에 따른 외부 서버 응답 차이가 있어요. 원본 URL과 복구 가능한 참조를 확인하고 이관본에 필요한 참조자 정책을 적용해요. |
| 긴 링크의 모바일 가로 넘침 | `222840790277`, `222831869103`, `221240210419`, `221615428797`, `222451585111`, `222476192402`. 최대 문서 폭은 데스크톱 3,433px, 모바일 2,944px였어요. | ps-blog 본문 링크의 줄바꿈 규칙이 없었어요. 이관 카테고리의 링크·문단에 CSS 줄바꿈을 적용하고 URL 문자열은 유지해요. 긴 태그가 있는 Bottom Navigation 글도 모바일 문서 폭 430px에서 390px로 줄었어요. 수식 내부 가로 스크롤은 유지해요. |

## 이미 이관 과정에서 처리한 항목

다음 문제도 같은 이관에서 확인했어요. 내보내기 엔진의 문제와 블로그가 요구하는 설정을 구분해 기록해요.

| 항목 | 처리와 확인 범위 |
| --- | --- |
| 원래 발행일 | 1,830개 글의 `date`와 `last_modified_at`을 원본 `publishedAt`으로 복원했어요. 파일명과 공개 URL은 유지했어요. |
| Liquid 문법 | 코드·본문에 Liquid 구문이 있는 글 28개는 Jekyll의 Liquid 처리를 끄고 원문을 보존했어요. |
| 제목 frontmatter | 여러 줄 제목 27개와 날짜처럼 보이는 제목을 YAML 문자열로 보존했어요. |
| 내부 링크 | 126개 글에서 이관 글 또는 기존 중복 글의 실제 ps-blog 주소로 연결했어요. |
| 넓은 표 | 68개 표에 키보드로 접근할 수 있는 가로 스크롤을 적용했어요. |
| 기존 외부 이미지 | Naver 스티커 378개에 이미지별 참조자 정책을 적용했어요. 실패한 카드 미리보기 65개는 링크를 남겼어요. Wikimedia SVG 5개 참조는 원본 주소를 복구하고 밝은 배경을 적용했어요. |
| 복구하지 못한 외부 이미지 | 독립 이미지 23개 참조는 조사 당시 오류·빈 이미지·시간 초과가 났어요. 재검사에서도 본문 이미지를 복구할 수 없어 원본 링크를 남겼어요. 세부 응답은 아래 검증 결과에 기록했어요. |
| 카테고리 목록의 요약 | 원문 문장으로 요약 1,719개를 만들고 나머지는 빈 요약으로 두어 자동 추출한 수식이 목록을 넘치지 않게 했어요. |

## 확인을 분리한 사항

- `222174268702`는 manifest에 성공으로 기록돼 있지만 원본 내보내기 폴더에 파일이 없어요. 출력 경로 중복은 없었어요. 현재 자료만으로 파일이 없어진 원인을 확인할 수 없어 이번 파서 결함으로 단정하지 않아요.
- 원본이 링크만 있는 글과 기존 글로 연결되는 카드는 의도적으로 이관하지 않았어요. 알고리즘 글이 적게 들어온 이유 중 하나예요.
- 외부 서버의 이미지 응답과 원본 영상 재생 가능 여부는 파서의 정보 보존과 별도로 확인해요. 썸네일이나 원본 링크 보존을 영상 파일 다운로드 성공으로 표시하지 않아요.
- KaTeX 오류가 0개여도 원본 수식이 빠졌을 수 있어요. 수식 오류 수와 함께 원본 수식 개수·내용을 비교해요.
- 빌드 성공만으로 화면 검증을 대체하지 않아요. 본문 코드·표·수식·이미지와 모바일 문서 폭을 실제 렌더링에서 확인해요.

## 검증 결과

- 원본 수식 조사에서는 235개 글의 842개 컴포넌트에 3,584줄이 있었어요. 이관한 글 중 복구 대상은 148개 글의 532개 컴포넌트였고, 532줄에서 3,272줄로 복원했어요. 추가된 수식은 2,740줄이에요.
- 이관 글 470개를 수정했어요. 전체 1,830개의 파일명·ID·원래 발행일과 frontmatter, 기존 fenced code 4,084개를 보존했어요. 원본 SQL 코드 6개를 추가해 총 4,090개가 됐어요.
- SQL 표를 코드로 복원하기 전, 이관 대상 1,830개를 수정한 파서로 다시 내보내 fenced code를 비교했어요. 기존 파일에서 fenced code가 있는 1,402개 글의 4,084개 블록은 종결 개행 한 개를 제외한 내용과 개수가 같았어요. 이 비교에서 추가 교체 대상은 없었어요. 원문 코드가 일반 텍스트나 표로 내보내진 경로는 이 대조에 포함되지 않아요. 공백 압축과 placeholder 결함은 별도 재현 테스트로 수정했어요.
- 외부 dthumb URL 29개는 Chromium에서 `no-referrer`로 실제 이미지 크기가 생기는 것을 확인했어요. 이관한 26개 글의 37개 참조에 적용했어요.
- 독립 이미지 원본 23개도 `no-referrer`로 다시 열었어요. 21개는 로드 오류, 1개는 시간 초과, 1개는 1 × 1 대체 이미지여서 본문 이미지로 복구할 수 없었어요. 원본 링크를 유지했어요.

- `mise exec -- pnpm check:coverage`: 131개 파일의 638개 테스트를 통과했어요. 문장·라인 92.29%, 분기 85.40%, 함수 95.83%로 저장소 기준을 충족했어요.
- `check:fmt`, `check:lint`, `check:storybook`, `check:unused`, `build:server`, `build:ui`, `pnpm --filter @exitpress/web build:pages`를 통과했어요.
- 실제 Naver 글 4개의 회귀 자료를 추가하고, 기존 자료 32개를 원문·새 출력과 대조해 갱신했어요. 추가 자료는 캐시를 우회한 현재 원본 HTML로 다시 확인했어요.
- 로컬 `check:playwright`에서 21개를 통과했어요. 실제 업로드 1개는 로컬에 `EXITPRESS_UPLOAD_E2E_GITHUB_TOKEN`이 없어 실행하지 못했어요. [PR CI 실행](https://github.com/mym0404/exitpress/actions/runs/34755135727)에서도 21개가 통과했고, 업로드 1개는 등록된 토큰이 GitHub API에서 `401 Bad credentials`를 반환해 최초 실행과 재시도 모두 실패했어요. 이후 전용 토큰을 갱신했어요. 최신 재검증 결과는 [PR #65의 required-checks](https://github.com/mym0404/exitpress/pull/65/checks)에서 확인해요. CI의 단위 테스트·정적 검사·빌드는 통과했어요.

- 수정 후 원본 카테고리 71개의 표본 191개와 수식 글 2개를 더해 총 193개를 데스크톱·모바일에서 확인했어요. 386개 화면에서 원문과 코드·수식 개수를 대조하고 이미지·문서 가로 넘침을 확인했어요.
- SQL 코드 6개는 양쪽 화면에서 원문·DOM·복사용 데이터가 일치했어요. 모바일 문서 폭 390px을 유지하며 코드 내부 가로 스크롤과 Wrap 버튼을 확인했어요. ps-blog에서는 이 6개만 초기 줄바꿈을 끄고 기존 코드의 기본 설정은 유지해요.
- ps-blog의 `node --test tests/processors/shiki-wrap.test.mjs`는 기본 줄바꿈, 명시한 예외, 두 번 처리 후 상태와 원문 보존을 확인해요.
- ps-blog 전체 production build는 기본 Node 힙 한도 약 4GB에서 Shiki 처리 중 메모리 부족으로 중단됐어요. 기존 처리기가 전체 HTML의 DOM과 코드 문자열을 동시에 보유하는 구조를 확인했어요. `NODE_OPTIONS=--max-old-space-size=16384 mise exec -- pnpm build`로 재실행해 코드 5,508개 처리와 Pagefind 검색 색인까지 통과했어요. 이 설정값이 최소 필요 메모리라는 뜻은 아니에요.
- 최종 production 생성물에서 이관 글 1,830개의 경로·발행일 일치, 코드 4,090개와 KaTeX 오류 0개를 확인했어요. 실제 `/render-sample/` 양쪽 화면에서도 수식 14개, 코드 4개와 기본 줄바꿈·토글 동작을 확인했어요.
